---
// Gate Animation Component - Opening gates with invitation cover
---

<div id="gate-container" class="fixed inset-0 z-50 flex items-center justify-center overflow-hidden">
  <!-- Background Image -->
  <div id="gate-background" class="absolute inset-0 w-full h-full opacity-0">
    <img 
      src="/photos/gate_background.png" 
      alt="Gate Background" 
      class="w-full h-full object-cover object-center"
      loading="eager"
      fetchpriority="high"
    />
  </div>

  <!-- Main Content Overlay (visible before gates open) -->
  <div id="gate-content" class="relative z-10 flex flex-col items-center justify-between h-full w-full px-4 py-12 md:py-16 lg:py-20 pointer-events-auto opacity-0">
    <!-- Top Section: Title, Couple Names, and Monogram -->
    <div class="flex-shrink-0 text-center mt-4 md:mt-6 lg:mt-8 w-full">
      <h1 
        id="gate-title"
        class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-khmer-display text-[#FFD700] mb-4 md:mb-6 opacity-0"
        style="text-shadow: 0 4px 20px rgba(0, 0, 0, 0.9), 0 0 60px rgba(255, 215, 0, 0.8), 0 2px 4px rgba(0, 0, 0, 1); -webkit-text-stroke: 1px rgba(139, 0, 0, 0.3); line-height: 1.8; padding: 0.5rem 0;"
      >
        សិរីសួស្តីអាពាហ៍ពិពាហ៍
      </h1>
      <p 
        id="gate-names"
        class="text-xl sm:text-2xl md:text-3xl lg:text-4xl xl:text-5xl font-khmer-display text-[#8B0000] mb-6 md:mb-8 lg:mb-10 opacity-0"
        style="text-shadow: 0 3px 15px rgba(0, 0, 0, 0.8), 0 0 40px rgba(255, 215, 0, 0.6); line-height: 1.8; padding: 0.5rem 0;"
      >
        ល្មុត ភក្តី & ស្រ៊ន ឌីនីន
      </p>
      
      <!-- Monogram Image -->
      <div id="gate-monogram" class="mt-4 md:mt-6 lg:mt-8 flex justify-center items-center w-full opacity-0">
        <img 
          src="/photos/initial_text_with_frame.png" 
          alt="Couple Monogram" 
          class="w-40 h-40 sm:w-48 sm:h-48 md:w-56 md:h-56 lg:w-64 lg:h-64 xl:w-80 xl:h-80 object-contain drop-shadow-2xl"
          style="filter: drop-shadow(0 0 30px rgba(212, 175, 55, 0.5));"
          loading="eager"
          fetchpriority="high"
          width="320"
          height="320"
        />
      </div>
    </div>

    <!-- Spacer -->
    <div class="flex-grow"></div>

    <!-- Bottom Section: Button and Event Details -->
    <div class="flex-shrink-0 text-center mb-4 md:mb-6 lg:mb-8">
      <!-- Open Button -->
      <button
        id="open-gate-btn"
        class="group relative bg-gradient-to-br from-[#FFD700] via-[#D4AF37] to-[#B8860B] hover:from-[#D4AF37] hover:to-[#8B6914] text-white font-khmer text-xl sm:text-2xl md:text-3xl lg:text-4xl px-12 sm:px-16 md:px-20 lg:px-24 py-6 sm:py-7 md:py-8 lg:py-10 rounded-full shadow-2xl transition-all duration-300 hover:scale-110 hover:shadow-3xl border-4 border-white/50 mb-8 md:mb-10 lg:mb-12 opacity-0"
        style="line-height: 1.8;"
      >
        <span class="relative z-10 font-bold">បើកធៀបការ</span>
        <div class="absolute inset-0 rounded-full bg-white/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
      </button>
      
      <!-- Event Information -->
      <div id="gate-event-info" class="bg-black/40 backdrop-blur-md rounded-2xl px-6 sm:px-8 md:px-10 lg:px-12 xl:px-14 py-6 sm:py-7 md:py-8 lg:py-10 border-2 border-[#D4AF37]/50 shadow-2xl max-w-4xl mx-auto opacity-0" style="margin-bottom: 5;">
        <p 
          class="text-base sm:text-lg md:text-xl lg:text-2xl xl:text-3xl font-khmer text-[#FFD700] mb-3 sm:mb-4 md:mb-5"
          style="text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8); line-height: 1.8; padding: 0.25rem 0; margin-left: 0.5rem; margin-right: 0.5rem;"
        >
          ថ្ងៃសុក្រ ទី២៧ ខែកុម្ភៈ ឆ្នាំ២០២៦ វេលាម៉ោង ៥:០០នាទីល្ងាច
        </p>
        <p 
          class="text-sm sm:text-base md:text-lg lg:text-xl xl:text-2xl font-khmer text-[#FFF8DC] leading-relaxed"
          style="text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8); line-height: 1.9; padding: 0.25rem 0;"
        >
          នៅ គេហដ្ឋានខាងស្រី ស្ថិតនៅ ភូមិកោះដាច់ សង្កាត់កោះដាច់
        </p>
        <p 
          class="text-sm sm:text-base md:text-lg lg:text-xl xl:text-2xl font-khmer text-[#FFF8DC] leading-relaxed"
          style="text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8); line-height: 1.9; padding: 0.25rem 0;"
        >
          ខណ្ឌជ្រោយចង្វារ រាជធានីភ្នំពេញ
        </p>
      </div>
    </div>
  </div>
</div>

<script>
  import { gsap } from 'gsap';

  // Prevent scrolling on page load
  document.body.style.overflow = 'hidden';

  // Initialize GSAP timeline for entrance animations
  const entranceTimeline = gsap.timeline({
    onComplete: () => {
      // Add continuous animations to all elements after entrance completes
      
      // Title - Subtle floating with glow pulse
      gsap.to('#gate-title', {
        y: -8,
        duration: 3,
        repeat: -1,
        yoyo: true,
        ease: 'sine.inOut'
      });
      
      // Names - Gentle floating
      gsap.to('#gate-names', {
        y: -6,
        duration: 2.5,
        repeat: -1,
        yoyo: true,
        ease: 'sine.inOut',
        delay: 0.3
      });
      
      // Monogram - Gentle pulse animation
      gsap.to('#gate-monogram', {
        scale: 1.05,
        duration: 1.5,
        repeat: -1,
        yoyo: true,
        ease: 'power1.inOut'
      });
      
      // Event info - Subtle scale breathing effect
      gsap.to('#gate-event-info', {
        scale: 1.02,
        duration: 2,
        repeat: -1,
        yoyo: true,
        ease: 'sine.inOut'
      });
      
      // Button - Bounce animation
      gsap.to('#open-gate-btn', {
        y: -10,
        duration: 0.6,
        repeat: -1,
        yoyo: true,
        ease: 'power1.inOut'
      });
      
      // Button shimmer effect
      gsap.to('#open-gate-btn', {
        boxShadow: '0 0 40px rgba(255, 215, 0, 0.8), 0 0 80px rgba(212, 175, 55, 0.6)',
        duration: 1,
        repeat: -1,
        yoyo: true,
        ease: 'power1.inOut'
      });
    }
  });

  // Entrance Animation Sequence (from landing on the page)
  entranceTimeline
    // Fade in background
    .to('#gate-background', {
      opacity: 1,
      duration: 1,
      ease: 'power2.out'
    })
    // Fade in content container
    .to('#gate-content', {
      opacity: 1,
      duration: 0.5,
      ease: 'power2.out'
    }, '-=0.5')
    // Animate title from top with glow effect
    .fromTo('#gate-title', 
      {
        opacity: 0,
        y: -50,
        scale: 0.8
      },
      {
        opacity: 1,
        y: 0,
        scale: 1,
        duration: 1,
        ease: 'elastic.out(1, 0.5)'
      },
      '-=0.2'
    )
    // Animate couple names with slight delay
    .fromTo('#gate-names',
      {
        opacity: 0,
        y: -30,
        scale: 0.9
      },
      {
        opacity: 1,
        y: 0,
        scale: 1,
        duration: 1,
        ease: 'elastic.out(1, 0.5)'
      },
      '-=0.7'
    )
    // Animate monogram with rotation and scale
    .fromTo('#gate-monogram',
      {
        opacity: 0,
        scale: 0,
        rotation: -180
      },
      {
        opacity: 1,
        scale: 1,
        rotation: 0,
        duration: 1.2,
        ease: 'back.out(1.7)'
      },
      '-=0.8'
    )
    // Animate event info from bottom
    .fromTo('#gate-event-info',
      {
        opacity: 0,
        y: 50,
        scale: 0.9
      },
      {
        opacity: 1,
        y: 0,
        scale: 1,
        duration: 0.8,
        ease: 'back.out(1.2)'
      },
      '-=1.2'
    )
    // Animate button with special entrance
    .fromTo('#open-gate-btn',
      {
        opacity: 0,
        scale: 0,
        y: 30
      },
      {
        opacity: 1,
        scale: 1,
        y: 0,
        duration: 0.45,
        ease: 'back.out(2)'
      },
      '-=0.6'
    );

  // Button click handler for opening animation
  const openGateBtn = document.getElementById('open-gate-btn');
  const gateContent = document.getElementById('gate-content');
  
  if (openGateBtn) {
    openGateBtn.addEventListener('click', () => {
      // Disable button to prevent multiple clicks
      (openGateBtn as HTMLButtonElement).disabled = true;
      
      // Stop all ongoing animations
      gsap.killTweensOf('#gate-title');
      gsap.killTweensOf('#gate-names');
      gsap.killTweensOf('#gate-monogram');
      gsap.killTweensOf('#gate-event-info');
      gsap.killTweensOf('#open-gate-btn');
      
      const openingTimeline = gsap.timeline({
        onComplete: () => {
          // Fade out entire gate container
          const gateContainer = document.getElementById('gate-container');
          if (gateContainer) {
            gsap.to(gateContainer, {
              opacity: 0,
              duration: 1,
              ease: 'power2.out',
              onComplete: () => {
                gateContainer.style.display = 'none';
                // Re-enable scrolling after gates are fully removed
                document.body.style.overflow = 'auto';
                // Ensure we start at the top of the main content
                window.scrollTo({ top: 0, behavior: 'auto' });
                
                // Trigger event for main content to start animating
                document.dispatchEvent(new CustomEvent('gateOpened'));
              }
            });
          }
        }
      });

      // Opening Animation Sequence
      openingTimeline
        // Button press effect
        .to('#open-gate-btn', {
          scale: 0.9,
          duration: 0.1,
          ease: 'power2.in'
        })
        .to('#open-gate-btn', {
          scale: 1.1,
          duration: 0.2,
          ease: 'power2.out'
        })
        // Flash effect
        .to('#open-gate-btn', {
          opacity: 0,
          scale: 2,
          duration: 0.4,
          ease: 'power2.out'
        })
        // Fade out event info
        .to('#gate-event-info', {
          opacity: 0,
          y: 30,
          duration: 0.5,
          ease: 'power2.in'
        }, '-=0.4')
        // Spin and zoom out monogram
        .to('#gate-monogram', {
          opacity: 0,
          scale: 3,
          rotation: 360,
          duration: 1,
          ease: 'power2.in'
        }, '-=0.4')
        // Fade out names
        .to('#gate-names', {
          opacity: 0,
          y: -30,
          scale: 1.2,
          duration: 0.6,
          ease: 'power2.in'
        }, '-=0.8')
        // Fade out title
        .to('#gate-title', {
          opacity: 0,
          y: -50,
          scale: 1.3,
          duration: 0.6,
          ease: 'power2.in'
        }, '-=0.5')
        // Zoom and fade background
        .to('#gate-background', {
          scale: 1.2,
          opacity: 0,
          duration: 0.8,
          ease: 'power2.in'
        }, '-=0.6');
    });
  }
</script>
